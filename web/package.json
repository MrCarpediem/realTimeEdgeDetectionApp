{
  "name": "web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && mkdir -p dist/frames && cp -r src/frames/* dist/frames/ || true",
    "start": "npx serve dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "serve": "^14.2.1"
  }
}
